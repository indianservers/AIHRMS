window.__AI_HRMS_CONFIG__ = {
  // Keep empty for same-origin deployments that proxy /api and /uploads.
  // For split deployments, set for example:
  // apiBaseUrl: "https://api.yourdomain.com/api/v1",
  // uploadsBaseUrl: "https://api.yourdomain.com",
  apiBaseUrl: "",
  uploadsBaseUrl: "",
};
