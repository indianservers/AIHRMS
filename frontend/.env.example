# Local frontend development.
# The Vite dev server proxies /api and /uploads to this backend.
VITE_DEV_API_PROXY_TARGET=http://localhost:8001

# Optional build-time API URL. Prefer public/app-config.js for production,
# because it can be changed on the frontend server without rebuilding.
VITE_API_BASE_URL=
