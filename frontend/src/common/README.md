# Frontend Common

Shared UI, auth/session state, runtime config, API base client, and layout code
remain import-compatible under `src/components`, `src/store`, `src/services`,
`src/config`, `src/hooks`, and `src/lib`.

New app-specific pages and routes should live under `src/apps/<app-key>`.

